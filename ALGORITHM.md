# LianWall 壁纸选择算法分析与改造设计

本文档记录本轮关于壁纸选择算法的完整讨论过程：从问题起因、怀疑点、验证过程，到新算法的数学定义与实现建议。

目标不是立刻给出一份“看起来聪明”的算法，而是先明确现有算法究竟在做什么、问题出现在哪里、以及新算法要满足什么性质。

---

## 背景与起因

LianWall 当前的壁纸选择流程建立在如下三层结构上：

1. 将当前模式下的活跃壁纸映射到一个圆周向量空间。
2. 用固定的黄金角步进旋转指针。
3. 每次从当前可用壁纸中选出“离指针最近”的一张。

设计初衷是利用黄金角的均匀遍历性质，避免简单随机在短时间内出现强重复，从而更符合人类对“随机”的主观感受。

但在实际审视算法行为时，出现了一个关键怀疑：

> 指针明明是顺时针旋转的，为什么选择结果仍然可能回到指针左边？

这个怀疑进一步引出了三个具体问题：

1. 总壁纸数量会变化，圆周离散粒度也会变化。
2. 冷却机制会让最近的一批壁纸暂时失效。
3. 锁定机制不会删除壁纸，只会让它们在选择时失效。

这意味着：即使黄金角步进本身是固定的，实际参与选择的候选集合却始终在变化。

---

## 现有算法复盘

### 向量空间生成

当前空间生成逻辑不是按黄金角给每张壁纸排点，而是：

1. 先对扫描结果做一次洗牌。
2. 再把洗牌后的壁纸均匀铺到圆周上。

若当前活跃壁纸数为 $N$，则第 $i$ 张壁纸角度为：

$$
a_i = \frac{2\pi i}{N}, \quad i \in \{0,1,\dots,N-1\}
$$

这意味着空间本身是“均匀圆周 + 随机映射”，而不是“黄金角空间”。

### 指针推进

当前指针每轮按黄金角推进：

$$
p_{t+1} = (p_t + \gamma) \bmod 2\pi
$$

其中：

$$
\gamma = 2\pi\left(1 - \frac{1}{\varphi}\right) \approx 2.399963229728653
$$

这里 $\varphi$ 是黄金比例。

### 当前选择规则

当前选择器使用“最短角距离”规则。

定义顺时针距离：

$$
d_i^+(t) = (a_i - p_t) \bmod 2\pi
$$

定义逆时针距离：

$$
d_i^-(t) = (p_t - a_i) \bmod 2\pi
$$

定义对称最短距离：

$$
d_i(t) = \min\bigl(d_i^+(t), d_i^-(t)\bigr)
$$

设当前有效候选集合为 $A_t$，即未锁定且不在冷却中的壁纸集合，则当前算法选择：

$$
i_t = \arg\min_{i \in A_t} d_i(t)
$$

这条规则的关键性质是：

> 它完全不关心方向，只关心谁离指针最近。

因此，它并不是“顺时针扫描器”，而是“无方向最近邻选择器”。

---

## 用户提出的怀疑

本轮讨论的核心怀疑是：

> 如果指针在顺时针旋转，那么选择是否应当优先选择指针右侧的壁纸？

进一步细化为：

1. 第 $n$ 张壁纸播放后会进入 $m$ 轮冷却。
2. 黄金角推进后，理论上指针应当在圆周上持续顺时针前进。
3. 但如果右侧最近点因冷却或锁定失效，当前算法是否会转而选择左侧最近点？
4. 这种“翻到左边”的行为，是偶然还是算法定义本身就会导致？

这个问题非常关键，因为它直接决定了当前系统到底在模拟：

- 一个“顺时针推进的随机系统”，还是
- 一个“没有方向感的最近邻扰动系统”。

---

## 数学验证过程

### 验证 1：不考虑冷却时，当前算法是否也会向左选择？

答案是：会。

设圆周离散间距为：

$$
\Delta = \frac{2\pi}{N}
$$

当指针落在两个相邻点之间时，若其距离左侧点为 $r$，则距离右侧点为 $\Delta - r$。

当前算法会：

- 当 $r < \frac{\Delta}{2}$ 时选左侧点。
- 当 $r > \frac{\Delta}{2}$ 时选右侧点。
- 当 $r = \frac{\Delta}{2}$ 时左右平局。

由于黄金角步进与圆周离散间距之间一般不共振，长期来看，$p_t \bmod \Delta$ 会在 $[0, \Delta)$ 上近似均匀分布。因此在没有冷却和锁定时，当前算法的长期行为近似满足：

$$
P(\text{向左}) \approx P(\text{向右}) \approx \frac{1}{2}
$$

这说明：

> 当前算法天然会左右两边都选，且长期比例接近各一半。

换言之，向左选择并不是冷却机制造成的异常，而是当前选择规则的内在性质。

### 验证 2：冷却是否会导致“本该向右，却被迫向左”？

答案同样是：会。

在当前规则中，冷却和锁定只会从候选集合 $A_t$ 中删除若干点，但不会改变指针位置，也不会重建局部有效空间。

因此，当右侧最近点失效时，算法会直接回退到剩余候选中角距离最短的一张；如果这张恰好在左侧，就会发生方向翻转。

这类翻转不是罕见边角，而是现有规则在“候选被打洞”后的自然结果。

### 验证 3：规则量化对比

在小规模模拟中，比较了以下几类规则：

1. 当前对称最近邻
2. 严格顺时针
3. 顺时针优先、无右侧时回退
4. 左侧带惩罚的偏置规则

量化观察包括：

- 左翻率（flip rate）
- 平均跳跃角度
- 长期分布方差
- 覆盖到的唯一壁纸数

主要结论如下：

1. 当前规则的左翻率稳定在约 50%。
2. 严格顺时针的左翻率为 0，但轨迹过于“扫描化”。
3. 单纯的方向惩罚可以显著降低左翻率，但仍是确定性规则。
4. 若目标是“尽量像随机，同时保留顺时针方向感”，最合适的不是硬规则，而是顺时针偏置的概率选择。

---

## 为什么最终选择“顺时针偏置概率选择”

我们最终没有选择严格顺时针，也没有继续使用当前对称最近邻，而是选定如下方向：

> 使用顺时针偏置的概率采样器，在保留全局方向感的同时，仍允许局部随机扰动。

原因如下：

### 当前对称最近邻的问题

优点：

- 局部总是选最近点
- 数学形式简单

缺点：

- 没有方向感
- 左翻是常态而不是例外
- 在冷却/锁定作用下，轨迹会显得“来回跳”

### 严格顺时针的问题

优点：

- 行为最直观
- 完全符合“指针向前扫过”的解释

缺点：

- 太像扫描器
- 人眼更容易察觉规律性
- 随机感不足

### 顺时针偏置概率选择的优势

优点：

- 右侧近点拥有更高概率
- 左侧点仍可在特殊情况下被选中
- 行为比严格顺时针更自然
- 行为比当前规则更符合“指针顺时针推进”的直觉
- 可以通过参数连续调节“方向性”和“随机性”

这正好符合本项目对“欺骗人类记忆机制”的目标：

> 既不能太机械，也不能毫无方向。

---

## 新算法的数学定义

### 候选集合

设当前有效候选集合为：

$$
A_t = \{ i \mid i \text{ 未锁定，且不在冷却中} \}
$$

### 局部尺度归一化

为了避免总壁纸数、锁定数、冷却数变化时评分尺度失真，引入当前有效候选集合的平均角间距：

$$
\Delta_t = \frac{2\pi}{|A_t|}
$$

这里不是用“总空间大小”归一化，而是用“当前可选集合大小”归一化。

### 顺时针偏置分数

定义顺时针偏置分数：

$$
s_i(t)=
\begin{cases}
\dfrac{d_i^+(t)}{\Delta_t}, & d_i^+(t) \le d_i^-(t) \\
\dfrac{d_i^-(t)}{\Delta_t} + \lambda, & d_i^-(t) < d_i^+(t)
\end{cases}
$$

其中：

- $\lambda \ge 0$ 是左侧惩罚项。
- 当候选在右侧时，不加惩罚。
- 当候选在左侧时，额外增加一个固定惩罚。

性质如下：

1. 当 $\lambda = 0$ 时，退化为无方向偏置评分。
2. 当 $\lambda$ 足够大时，逼近严格顺时针选择。
3. $\lambda$ 越大，向左翻转的概率越低。

### 概率采样规则

不再直接取分数最小值，而是采用 softmax 概率采样：

$$
P(i\mid t)=
\frac{\exp\bigl(-s_i(t)/\tau\bigr)}{\sum_{j\in A_t}\exp\bigl(-s_j(t)/\tau\bigr)}
$$

其中 $\tau > 0$ 为温度参数。

性质如下：

1. $\tau \to 0$ 时，趋近于确定性选择最小分数候选。
2. $\tau$ 越大，概率分布越平坦，随机性越强。
3. $\tau$ 越小，算法越稳定、越接近“有方向的最优选择”。

因此，新算法可视为一个二维参数系统：

- $\lambda$ 决定方向偏置强度。
- $\tau$ 决定随机程度。

---

## 参数直觉

### $\lambda$：方向偏置强度

- 小 $\lambda$：左侧只是略吃亏，局部仍容易翻转。
- 中等 $\lambda$：右侧明显占优，但左侧在特别近时仍可能胜出。
- 大 $\lambda$：行为逐渐逼近严格顺时针。

### $\tau$：随机温度

- 小 $\tau$：几乎总选最优候选，轨迹更稳定。
- 中等 $\tau$：近点之间有概率竞争，最符合“看起来随机”。
- 大 $\tau$：选择趋于均匀随机，方向感被削弱。

---

## 推荐的第一版参数

基于本轮讨论，推荐第一版从以下参数开始：

$$
\lambda = 0.35, \quad \tau = 0.28
$$

这组参数的设计目标是：

1. 保留明显的顺时针偏置。
2. 允许局部扰动，看起来不死板。
3. 避免当前规则大约 50% 左右的左翻行为。
4. 避免严格顺时针那种过强的“扫描器感”。

后续可视需要进一步调参，但第一版实现应优先验证方向偏置的主效果，而不是一开始就引入过多策略分支。

---

## 与现有实现的差异

新算法不会改变以下内容：

1. 黄金角步进仍然保留。
2. 冷却机制第一版仍作为硬约束。
3. 锁定机制第一版仍只影响候选集合，不重排空间。

新算法会改变的核心点只有一个：

> “从当前有效候选集合里挑哪一张” 的规则。

也就是说，当前实现是：

$$
\arg\min d_i(t)
$$

而新实现会变成：

$$
\text{按 } P(i\mid t) \text{ 进行抽样}
$$

这是一种局部选择器替换，而不是整套空间模型推倒重来。

---

## 第一版实现边界

为了控制复杂度，第一版实现建议遵守以下边界：

### 保留硬冷却

先将冷却项从候选集中排除，再在剩余集合中做概率采样。

理由：

1. 保持当前“不短期重复”的硬保证。
2. 避免概率采样第一版就引入重复率失控问题。

### 不改变空间生成方式

第一版不修改：

- 洗牌逻辑
- 均匀圆周分布逻辑
- rebuild 的整体策略

理由：

当前问题的核心在选择规则，而不是指针步进或空间生成本身。

### 引入可复现的随机源

为了保证算法可测试、可持久化、可复现，第一版实现不建议直接调用系统随机数。

推荐做法：

1. 在 `WallpaperSpace` 中增加一个可持久化的采样步计数器（如 `selector_nonce`）。
2. 每次选择时，根据 `pointer + selector_nonce + mode salt` 派生本轮采样随机值。
3. 每轮选择后递增该计数器。

这样可以兼顾：

- 概率选择
- 可复现性
- 测试稳定性

---

## 结论

本轮讨论最终确认了以下事实：

1. 当前算法并不是顺时针扫描器，而是无方向最近邻选择器。
2. 向左翻转不是边缘 bug，而是当前规则的数学必然结果。
3. 冷却和锁定会让这种翻转变得更不规则、更明显。
4. 若目标是“尽量像随机，同时保留顺时针方向感”，最佳改造方向不是严格顺时针，而是顺时针偏置的概率选择。

因此，LianWall 下一阶段的算法改造应当围绕如下目标展开：

> 用顺时针偏置概率采样替换当前的对称最近邻选择规则，并保留黄金角步进与硬冷却机制。

---

## 后续实现建议

建议后续代码实现按如下顺序推进：

1. 在核心算法层新增顺时针偏置评分与 softmax 采样逻辑。
2. 在 `WallpaperSpace` 和持久化结构中增加概率选择所需的稳定随机状态。
3. 在配置中增加算法参数：
   - `strategy`
   - `bias_lambda`
   - `temperature`
4. 保持 daemon 调用路径不变，只替换核心选择器。
5. 补充测试，验证：
   - 左翻率明显下降
   - 冷却窗口内无重复
   - 所有候选长期仍能被覆盖
   - 重启后行为可复现

当本文档中的数学定义与实现边界被确认后，即可开始进入代码实现阶段。